#usefull anaconda commands
# Create the environment named 'golf_env' with the latest Python
conda create -n golf_env python=3.14

# Activate it (Crucial step!)
conda activate golf_env


script anaconda to environment
C:\Users\barda\anaconda3\Scripts\activate.bat 

%windir%\System32\cmd.exe "/K" C:\Users\barda\anaconda3\Scripts\activate.bat C:\Users\barda\anaconda3 && conda activate golf_env


git update for github web link
Terminal: flet publish main.py --base-url /golfe-results/

Manual Step: Copy index.html to 404.html inside the dist folder.

Terminal: git add .

Terminal: git commit -m "Corrected base-url"

Terminal: git subtree push --prefix dist origin gh-pages